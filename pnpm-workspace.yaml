# Approve build scripts for these deps (they only validate prebuilt binaries).
# pnpm 11 uses the allowBuilds map; kept onlyBuiltDependencies for older pnpm.
allowBuilds:
  esbuild: true
  unrs-resolver: true
onlyBuiltDependencies:
  - esbuild
  - unrs-resolver
